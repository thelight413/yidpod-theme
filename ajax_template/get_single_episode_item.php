<h1>Coming Soon..</h1>
<?php

/*
    $podcasturl = $_POST['podcasturl'];
     $ID = url_to_postid( $podcasturl );
  
    global $wpdb;
    $table_name = $wpdb->prefix."episode";
   $post_id = get_query_var( 'post_id' );
   $sql = "SELECT * from  $table_name WHERE podcast_id='$ID' order by published_date DESC ";
   $results = $wpdb->get_results($sql);

   
   foreach ($results as $key => $value) {
   // $titlename = RemoveSpecialChar($value->title);
     print_r($value);
   }      
   //echo $titlename;  

   */